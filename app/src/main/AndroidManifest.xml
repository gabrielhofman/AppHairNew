<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apphairnew">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.CadastroServico">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.TesteCep" />
        <activity android:name=".ui.DashBoard">

        </activity>
        <activity android:name=".ui.NovoContato" />
        <activity android:name=".ui.CadastroUsuario" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.Login" />
    </application>

</manifest>